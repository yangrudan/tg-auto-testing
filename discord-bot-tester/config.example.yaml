discord:
  token: "YOUR_BOT_TOKEN_HERE"  # 替换为你的 Discord Bot Token
  bot: "your_bot_name"           # 替换为你的 Bot 用户名（不含 #0000）
  guild_id: 123456789012345678   # 替换为目标服务器 ID（可选，默认使用第一个服务器）
  channel_name: "test-channel"   # 替换为目标频道名称（可选，默认使用第一个文本频道）